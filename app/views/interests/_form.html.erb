<%= form_for(@interest) do |f| %>
  <% if @interest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interest.errors.count, "error") %> prohibited this interest from being saved:</h2>

      <ul>
      <% @interest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, :class=>"form-control" %>
  </div>


  <input type="hidden" name="organization_id" value="<%= params["id"] %>"/>

  <div class="field">
    <%= f.label :desc %><br>
    <%= f.text_area :desc, :class=>"form-control" %>
  </div>

  <div class="actions">
    <%= f.submit(:class=>"btn btn-primary buffer-top form-control") %>
  </div>
<% end %>
