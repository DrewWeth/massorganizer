<%= form_for(@device_submission) do |f| %>
  <% if @device_submission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@device_submission.errors.count, "error") %> prohibited this device_submission from being saved:</h2>

      <ul>
      <% @device_submission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :device_id %><br>
    <%= f.number_field :device_id %>
  </div>
  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
